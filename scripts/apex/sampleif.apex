string s1=' ';
if(s1==''){
    system.debug('S1 is Blank.Okay ?');

}else{
    system.debug(s1);
}
if(string.isBlank(s1)){
    system.debug('S1 is Still Blank.Okay ?');
}else {
    system.debug(s1);
}